//Import Components
import Nav    from './nav';
import Routes from './routes';

const Body = () => {
  return (
    <div className="relative bg-black min-h-screen bg-image-chip">
      <Nav />
      <Routes />
    </div>
  )
}

export default Body;
